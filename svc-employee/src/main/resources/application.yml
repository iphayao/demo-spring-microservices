server:
  port: 8081

spring:
  application:
    name: employee-service
  sleuth:
    sampler:
      probability: 1.0

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka